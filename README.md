# Accurate_Delta_ZIP_Copy_Automation
A script to detect and copy only changed ZIP files (or files inside ZIP) from a source folder to a shared folder, ensuring content accuracy, not just relying on timestamps. 
